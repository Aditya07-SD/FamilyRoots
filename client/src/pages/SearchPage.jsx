import{useState}from"react";import{Search,Users,CalendarDays,ArrowRight}from"lucide-react";import{api}from"../lib/api";import{Link}from"react-router-dom";
export function SearchPage(){const[q,setQ]=useState(""),[results,setResults]=useState([]),[busy,setBusy]=useState(false);const go=async e=>{e.preventDefault();if(!q.trim())return;setBusy(true);try{const r=await api.get("/members/search?q="+encodeURIComponent(q));setResults(r.data.data||[])}finally{setBusy(false)}};return <div className="feature-page"><div className="page-heading"><div><p className="eyebrow">FAMILY SEARCH</p><h1>Find your family</h1><p className="muted">Search members by name, relationship, occupation or place.</p></div></div><form className="search search-page-box" onSubmit={go}><Search size={20}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search your family…" autoFocus/><button className="primary">{busy?"Searching…":"Search"}</button></form><div className="search-section"><div className="section-heading"><div><p className="eyebrow">MEMBERS</p><h2>People</h2></div><span>{results.length} result{results.length!==1?"s":""}</span></div>{results.length?<div className="member-grid">{results.map(m=><article className="member-card" key={m._id}><div className="member-photo">{m.photoUrl?<img src={m.photoUrl} alt=""/>:m.fullName[0]}</div><div className="member-info"><h3>{m.fullName}</h3><p>{m.occupation||"Family member"}</p><small>{m.location||"—"}</small></div><Link className="icon-btn" to="/tree" title="Open family tree"><ArrowRight/></Link></article>)}</div>:<div className="empty-state"><Search size={28}/><h3>Search across your family</h3><p>Members are indexed now; events and memories can join the global search as those workspaces are populated.</p></div>}</div></div>}
