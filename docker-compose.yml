services:
  mongodb:
    image: mongo:8
    container_name: familyroots-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - familyroots_mongo:/data/db
volumes:
  familyroots_mongo:
